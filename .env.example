# 项目配置示例文件

# 环境标识: prod (生产环境), debug (调试环境)
RUN_ENV=debug

# 项目名称: 用于日志文件名和 S3 路径前缀
PROJECT_NAME=your_project_name

# AWS S3 配置 (如果需要使用 S3 功能)
AWS_ACCESS_KEY_ID=your_access_key
AWS_SECRET_ACCESS_KEY=your_secret_key
AWS_DEFAULT_REGION=your_region
